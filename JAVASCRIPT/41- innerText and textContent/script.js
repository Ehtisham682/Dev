const para = document.querySelector('p')

console.log(para.innerText) //<b> is display hidder to it wont show in innerText
// visibility:Hidden also works the same, it will hide in innerText

console.log(para.textContent) // textContent will show the content exactly as written in code. even blank spaces.
